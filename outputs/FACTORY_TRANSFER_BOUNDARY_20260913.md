# 经验迁移与边界任务包

后续运行更新：T4 三组已完成原生对照；T3 首次尝试因额度中断，第二次尝试三组已完成并通过。详见 [公开结果索引](../docs/V5_NATIVE_EVALUATION_INDEX.md)。原始实测记录仅保留在本地归档中。下文保留任务包刚准备完成时的原始状态。

## 本轮交付

围绕原定 T3/T4，完成两个开发变体、六份 A/B/C 工作目录、外部验收和控制器入口。
T3 把历史导入规则迁移到“物流回执”的新字段；T4 则明确要求严格消息保留原文，不能套用导入清洗规则。
每份项目有 8 个源文件，使用 Node 内置库，没有数据库、页面或新增依赖。

- 三组均可读当前规范、历史说明及修复前后源码，同题三组业务输入哈希一致。
- C 组只额外获得从同一历史材料预置的 Skill；不冒充现场学习。
- 应用前显式选择经验或不应用，记录理由、目录补查和注入字节；不按题号自动作答。
- 派发接入上一轮的共享预算。实际宿主派发、等待、关闭仍由控制器调用，脚本不伪造执行。
- 外部判定、Factory 自身状态与预算结果分别保存。

## 当前观察

| 任务 | 本地词面召回 | 可以说明什么 |
| --- | --- | --- |
| T3 换说法迁移 | NO_LEXICAL_MATCH，0 候选 | 当前词面规则会漏掉这份同义业务任务；应观察目录补查是否有用及其代价 |
| T4 不适用边界 | 返回 1 个候选，同时标出边界重叠 | 候选召回不能代替适用性判断；尚未发生经验误用，不能算作 Agent 失败 |

两份参考实现通过外部验收，六份未实现或故意写错的校准实现被拒绝。
任务包集成检查及 eval:typecheck 通过。修复了一处校准子进程继承 Node 测试环境、导致跳过验收的问题；这是新增评测脚本的问题，不是受测 Agent 的失败。

尚无新增模型实测、成功率、费用或速度结论。原来六次试跑没有重跑、删除或改写。
本任务包是公开开发变体，不能宣传为未见正式基准；其余四题仍在既定方案中。

## 运行与产物

启动命令：在 packages/factory-cli 下运行 `npm run eval:transfer`。只准备任务，不启动 Agent。

本次已准备的清单：
`<temp>/factory-transfer-boundary-vALgTe/controller/manifest.json`

代码与操作说明：`packages/factory-cli/evals/transfer-boundary/README.md`。
所有六份预算仍未启动，C 组选择也尚未填入；可以从此清单继续，不需重新造题。

下一步在干净上下文中执行这两题的受控对照，保留失败和额外成本，重点观察补查与边界判断。准备阶段的发现不直接升级成生产语义检索实现，不通过改题制造优势。
