# F: Default Workflow Integration — R1 embeds Default Startup Protocol as Step 0. Multi-Agent Decision Gate runs before code. Cleanup PLAN-first enforced. Path handoff mandatory.
# G: Multi-Agent Integration — Role profiles loaded on-demand. Contracts/handoffs validated by Integrator. projectId mandatory. Never auto-start.
# H: Isolation/Lifecycle/Cleanup Integration — Project identity checked at mount. Lifecycle state governs ops. Cleanup scoped to projectId. Foreign context blocked.
# I: Dashboard/Recovery/Evidence Integration — Dashboard shows lifecycle state + evidence levels. Recovery scan at startup. Evidence validator blocks overclaim.
