# RC0 User Review Guide

> **⚠️ IMPORTANT: This is a RELEASE CANDIDATE, not a release. Do not use in production.**

---

## What RC0 Is

RC0 (`codex-factory-core-v0.9.0-pre-RC0.zip`) is a **release candidate package** for the Codex App Factory.
It is a snapshot of the factory tooling at P8 staging level, intended for:

- **Smoke testing** — verify the package extracts and CLI works
- **User review** — inspect the contents and structure
- **Pre-v0.5 evaluation** — understand what would be in v0.5

## What RC0 Is NOT

- ❌ **NOT a release** — not v0.5, not production-ready
- ❌ **NOT deployable** — no production deployment capability
- ❌ **NOT a final product** — v0.5 is still BLOCKED
- ❌ **NOT proof of production readiness** — BLOCK-002/003 active
- ❌ **NOT proof of Factory superiority** — limited project type coverage

---

## How to Review RC0

### 1. Locate the Package

```
outputs/codex-factory-core-v0.9.0-pre-RC0.zip
outputs/codex-factory-core-v0.9.0-pre-RC0.zip.sha256
```

### 2. Verify SHA256

```powershell
Get-FileHash -Path "outputs/codex-factory-core-v0.9.0-pre-RC0.zip" -Algorithm SHA256
```

Expected: `A058FD67A02B13AAB010040905FA1D471050F9CB7A4A07EB3C5B9C5DB9240EF6`

### 3. Extract to a Clean Directory

```powershell
Expand-Archive -Path "outputs/codex-factory-core-v0.9.0-pre-RC0.zip" -DestinationPath "C:\rc0-review"
```

### 4. Inspect Key Files

| File | What to Check |
|---|---|
| `AGENTS.md` | Factory bootstrap rules intact |
| `factory-release/RC_BOUNDARY.md` | RC boundary definition |
| `factory-release/RC_CRITERIA.md` | RC criteria |
| `factory-release/RC0_METADATA.json` | Flags: releaseAllowed=false, rcCandidate=true |
| `scripts/factoryctl.ps1` | Factory CLI entry point |
| `governance/` | All governance modules present |
| `factory-build-mode/memory-quality/` | Memory quality policies present |

### 5. Run Help (Dry Run Only)

```powershell
# In the extracted directory
Get-Help .\scripts\factoryctl.ps1
```

### 6. Check Release Flags

Open `factory-release/RC0_METADATA.json` and verify:
- `releaseAllowed: false`
- `v05Package: false`
- `finalRelease: false`
- `rcCandidate: true`
- `artifactType: "RELEASE_CANDIDATE"`

---

## What Remains Before v0.5

| Blocker | Status | What's Needed |
|---|---|---|
| BLOCK-001 | STRONG_PARTIAL_EVIDENCE | More project type coverage for AB trials |
| BLOCK-002 | ACTIVE | Production deployment validation |
| BLOCK-003 | ACTIVE | Bridge local dev → production readiness |
| BLOCK-004 | ACTIVE | Your explicit approval for v0.5 |

**v0.5 is BLOCKED until all four blockers are resolved with your explicit approval.**

---

## Known Limitations of RC0

1. **Memory quality** is in `factory-build-mode/memory-quality/`, not yet a standalone top-level module
2. **No Native Build Pro** — built with Build Lite only
3. **No production deployment** — BLOCK-002/003 active
4. **Experimental modules** (codex-factory-plugin/, monitoring/) included with caveats
5. **`.env.example`** templates in `runnable-starters/` are safe templates, not real secrets

---

## Next Steps After Review

1. **If RC0 looks good:** Proceed to RC-USER-REVIEW-0 or RC-SMOKE-1
2. **If issues found:** Report issues, RC can be recreated
3. **Do NOT:** Mark v0.5 as released, deploy to production, or treat RC0 as final

---

*Generated by FACTORY-RELEASE-CANDIDATE-0, 2026-06-28*
