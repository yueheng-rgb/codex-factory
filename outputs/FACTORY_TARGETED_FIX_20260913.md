# Factory 定向优化完成

本轮完成上一轮开发试跑暴露的命令误判修复，以及经验注入文本精简。未派发新 Agent，未重跑六组对照，也未启动十八次正式评测。

## 变更

- 新回执采用 `rg-files-v1`：仅将可识别的独立 `rg --files` 的退出 1 归为 `NO_MATCH`，保留原始命令及退出码。
- 验收命令、退出 2 等执行错误、未知选项、shell 包装/插值/复合命令仍失败；没有通用的“忽略非零”开关。
- 写入回执、恢复时重验回执、准备修复上下文共用判定。修复提示不会把新策略下的无匹配搜索当作需要修复的错误。
- 旧回执没有策略标记时保持严格规则。实际读取原 `pilot-t1-b` 返回 `FAILED`、完整性 `VERIFIED`；原始 FAIL 文件与归档逐字节一致。
- 经验注入保留描述、规则及理由、适用与禁用条件、正反例、来源、Skill 路径和完整文件哈希，去掉重复 YAML、通用发布说明及与规则完全相同的理由。发布的 Skill 与批准哈希不变，既有任务与决策不被重写。

## 实际验证

- TypeScript 构建通过。首次构建发现类型收窄问题，修正后构建通过。
- 23 项定向检查通过：命令分类 4、原有教学与精简兼容检查 9、回执集成 4、修复上下文 3、经验筛选兼容检查 3。
- 实际重新执行历史 `rg --files` 诊断，退出 1、stdout/stderr 为空；新分类为 `NO_MATCH`，旧分类仍为 `FAILURE`。这不是一次新的 Agent 对照。
- 同一历史经验、原始任务与适用理由下，任务 JSON 从 **3,719 字节降至 2,803 字节**，减少 **916 字节（24.63%）**。Skill 文件哈希保持不变。
- 未运行全部测试或扩大审计。全工作区 `git diff --check` 指出未触及的旧 `RUNTIME_VALIDATION_REPORT.md` 有行尾空格；保留原文件，没有顺手修改。

## 限制与下一步

命令判断仍基于已记录的命令文本和退出码，不证明宿主实际执行身份；识别器刻意只支持窄范围的独立文件搜索，不是通用 shell 解析器。
字节减少只覆盖派发任务 JSON；如果 Worker 继续读取完整 Skill，整体 Token 变化需要另测。没有把本轮结果包装为成功率、费用或端到端速度提升。
下一阶段再进入正式任务包准备：先确定可执行的预算限制和度量口径，再冻结新变体。当前二十分钟约束仍仅由提示词传达，不能据此声称严格同预算性能结果。

[结构化验证记录](FACTORY_TARGETED_FIX_20260913.json)

复现定向检查：在 `C:/Codex_App_Factory/packages/factory-cli` 执行 `npm run build`，随后执行 `node --import tsx --test tests/command-outcome.test.ts tests/teaching.test.ts`；回执与修复上下文检查使用同目录下的 `control-plane.test.ts`、`repair-preparation.test.ts` 中相关测试。
