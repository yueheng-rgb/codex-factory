# Codex Capability Scorecard

> 用来评估 Codex 在项目生成各环节的稳定性和专业度。每项 0-5 分。

---

## 1. 需求理解能力

**0 分表现**:
- 看到一句话需求直接写代码，不做预分析
- 忽略用户明确说的"不要数据库"
- 把落地页需求理解成"做个网站"，加上登录和后台

**3 分表现**:
- 能识别核心目标和不做什么
- 能区分前端需求和全栈需求
- 能在开工前归纳用户角色和路径

**5 分表现**:
- 能从模糊需求中提取隐藏约束（如"这个不需要后台"）
- 能在用户说"做个管理系统"时追问管理员是谁、管什么
- 能区分真实业务需求和用户随口说的"以后也可能要"

**典型失败案例**:
> 用户: "帮我做一个课程报名管理系统"
> Codex: 直接创建 Next.js + Prisma + NextAuth 项目，第一天就建了 15 表

**验证方法**:
用 benchmark 假需求让 Codex 预分析，检查是否跳过 Project Expertise Flow

---

## 2. 项目类型判断能力

**0 分表现**:
- 所有需求都套同一个 starter
- 把 landing page 判成 fullstack-admin
- 把 AI tool 判成 API service

**3 分表现**:
- 能正确区分 5 种项目类型
- 能说明为什么不选其他类型
- 切换需求时能切换 starter 选择

**5 分表现**:
- 对混合需求能说"这是 A+B，但第一阶段应该先做 A"
- 能判断需求类型是否在已有 starter 覆盖范围内
- 如果不在，能建议从哪个最近 starter 裁剪

**典型失败案例**:
> 用户: "帮我做一个 AI 文案生成网站"
> Codex: 选择 fullstack-admin starter，把文案生成做成表单字段

**验证方法**:
用 8 个 benchmark 项目依次测试，检查类型判断准确率

---

## 3. 架构体量判断能力

**0 分表现**:
- 所有项目都按 XL 架构做（微服务、消息队列、多租户）
- 所有项目都按 S 架构做（没考虑扩展）
- 不知道何时"暂不升级"

**3 分表现**:
- 能判断 S/M/L/XL
- 知道 S 级项目不需要微服务
- 知道什么时候说"先不升级"

**5 分表现**:
- 能指出当前体量设计和下一档升级条件
- 能说"现在是 M，当用户量达到 X 时需要升级到 L"
- 能平衡"不过度设计"和"不留技术债"

**典型失败案例**:
> 3 个页面的内容站，Codex 设计了 Nginx + Docker + K8s + CI/CD pipeline

**验证方法**:
检查 Architecture Scaling Ladder 输出，看体量判断是否合理

---

## 4. Starter 选择能力

**0 分表现**:
- 不读 STARTER_REGISTRY
- 随机选 starter
- 选了不合适的 starter 后强行改造

**3 分表现**:
- 正确选择 starter
- 能说明为什么不选其他 starter
- 复制后基础检查通过（package.json name 替换、无 PROJECT_NAME 残留）

**5 分表现**:
- 对相似 starter 能做 A/B 对比（如 fullstack-admin vs saas-ai-tool）
- 能判断项目是否需要组合 starter
- 如果 starter 不完全匹配，能说明需要改造哪些部分

**典型失败案例**:
> 选择了 saas-ai-tool 后把 AI 功能全部删掉，本质上用错了 starter

**验证方法**:
每个 benchmark 项目检查 starter 选择是否正确、理由是否充分

---

## 5. 项目最小闭环能力

**0 分表现**:
- 第一阶段就做全部功能
- 没定义"最小闭环"概念
- 把原型做成正式产品

**3 分表现**:
- 能定义第一阶段最小可用路径
- 明确列出暂不实现内容
- 不过度设计

**5 分表现**:
- 能写出精确的用户路径（"管理员登录 → 看到列表 → 点一条进去 → 改状态 → 回到列表看到变化"）
- 知道 mock 和真实实现的边界
- 能力升级时说出明确触发条件

**典型失败案例**:
> 报名系统第一阶段就做了短信通知、Excel 导出、数据大屏、微信支付

**验证方法**:
检查 PROJECT_BRIEF.md 是否定义了最小闭环和暂不实现项

---

## 6. 工程稳定性

**0 分表现**:
- npm install 失败
- typecheck 不通过
- build 报错
- dev server 启动秒崩
- 靠乱加依赖乱改 tsconfig 强行通过

**3 分表现**:
- install / typecheck / build / dev 全部通过
- 依赖仅包含 starter 原有
- 未引入不必要的重型依赖

**5 分表现**:
- 所有检查第一次通过（零修复）
- 如果出错，能正确判断问题来源（代码问题 vs starter 缺陷 vs 环境问题）
- 修复后不引入新问题

**典型失败案例**:
> build 报错后加了 5 个 `@ts-ignore`、把 `strict` 改成 `false`、新增 3 个依赖

**验证方法**:
每个 benchmark 项目执行 install / typecheck / build / dev，统计通过率和首次通过率

---

## 7. 代码架构质量

**0 分表现**:
- 所有代码写在 App.tsx 一个文件
- 业务逻辑和 UI 混在一起
- mock 数据和真实 API 调用混在一起

**3 分表现**:
- 组件/数据/工具分层清楚
- 业务数据集中在 data/ 目录
- API 统一返回格式
- 内容不硬编码在组件

**5 分表现**:
- 分层后保留升级路径（mock → 真实 API 时不需要改 UI 组件）
- 组件可复用且有合理的 props interface
- 能看出这是"做过很多项目的团队写的代码"而不是"AI 一次生成的代码"

**典型失败案例**:
> Features 组件里硬编码了 6 个 feature 的 title/description，改内容必须改组件源码

**验证方法**:
检查组件是否从 siteContent 读取数据、API 是否有统一格式、是否有 guards

---

## 8. 安全与权限意识

**0 分表现**:
- API key 写在前端代码
- 权限用前端 `if` 判断
- 数据库密码硬编码

**3 分表现**:
- 知道 API key 只能放服务端环境变量
- 权限校验在服务端 API 中
- 前端只是 UI 隐藏，不是权限控制

**5 分表现**:
- 能把 mock auth placeholder 做成可替换接口
- 注释清楚哪些地方在接真实认证后需要改
- 考虑 CSRF / 输入校验 / 敏感操作审计

**典型失败案例**:
> `lib/ai-provider.ts` 里写了 `const API_KEY = "sk-xxx"` 放在前端代码里

**验证方法**:
搜索项目中是否有 `API_KEY`、`sk-`、`password` 等敏感词出现在前端代码

---

## 9. 数据一致性意识

**0 分表现**:
- 模拟数据库就是一个数组，没有唯一约束
- 没有考虑并发问题
- 没有审计字段（createdAt/updatedAt）

**3 分表现**:
- 知道 mock-db 和真实数据库的区别
- 注释说明正式项目需要事务/唯一约束/原子更新
- 有基本的 createdAt/updatedAt 字段

**5 分表现**:
- 能说明每个写操作为什么需要事务
- 能设计幂等 key
- 知道扣减操作需要原子更新
- 在代码中留了升级接口

**典型失败案例**:
> 扣减额度逻辑是 `user.quota = user.quota - 1`，没有事务保护

**验证方法**:
检查 usage-quota.ts 是否有事务注释、扣减是否有失败回滚、是否有幂等占位

---

## 10. 问题回流能力

**0 分表现**:
- 出错了不知道谁的问题
- 在测试项目上打补丁，不修复源 starter
- 同一个问题反复出现

**3 分表现**:
- 能判断问题来源（生成项目 vs starter 源模板 vs 复制脚本 vs 环境）
- 如果是 starter 缺陷，同步修复源模板
- 记录了发现的问题

**5 分表现**:
- 修复后更新 RUNTIME_VALIDATION_REPORT / QUALITY_CHECKLIST / scripts README
- 形成预防规则，防止同类问题再次发生
- 让后续项目天然受益于之前的修复

**典型失败案例**:
> BOM 问题在 3 个不同项目中出现 3 次，每次都在测试项目上手动去 BOM，从未修复制脚本

**验证方法**:
检查 runtime validation 报告是否有问题回流记录，是否同步修了 starter

---

## 评分总览

| 维度 | 当前估计评分 | 目标评分 |
|---|---|---|
| 1. 需求理解能力 | — | 4+ |
| 2. 项目类型判断 | — | 4+ |
| 3. 架构体量判断 | — | 4+ |
| 4. Starter 选择 | — | 4+ |
| 5. 最小闭环能力 | — | 4+ |
| 6. 工程稳定性 | — | 5 |
| 7. 代码架构质量 | — | 4+ |
| 8. 安全与权限意识 | — | 4+ |
| 9. 数据一致性意识 | — | 4+ |
| 10. 问题回流能力 | — | 4+ |

> 当前评分留空，待跑完 benchmark suite 后填入。

---

## 评分标准速查

| 分数 | 含义 |
|---|---|
| 0 | 完全失败 — 不知道这个维度存在 |
| 1 | 意识到但做不对 — 知道要判断类型但判错 |
| 2 | 偶尔做对 — 靠运气 |
| 3 | 基本稳定 — 大多数情况对，偶尔失误 |
| 4 | 稳定可靠 — 所有场景都对，有理有据 |
| 5 | 专家级 — 能教别人，能在边界情况下做出正确判断 |