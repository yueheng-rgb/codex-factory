# Update Memory After Phase

```powershell
.\factory-build-mode\runtime\memory\scripts\update-state.ps1 -ProjectPath "{projectPath}" -Stage "{stage}"
.\factory-build-mode\runtime\memory\scripts\append-decision.ps1 -ProjectPath "{projectPath}" -Decision "{decision}" -Rationale "{rationale}"
```
