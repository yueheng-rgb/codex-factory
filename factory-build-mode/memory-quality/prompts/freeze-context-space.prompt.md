# Freeze Context Space
Freeze project {project_id}:
1. Require user confirmation
2. Preserve ALL data
3. Set status=FROZEN
4. Disable auto-mount
5. Generate freeze.json metadata
6. Generate cleanup report
7. Tell user: "Frozen. All data preserved. No auto-mount. Fully reversible."
