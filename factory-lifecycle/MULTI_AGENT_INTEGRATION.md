# MULTI_AGENT_INTEGRATION.md
> Multi-agent ONLY on ACTIVE state (with user confirmation). Blocked on all others. If lifecycle changes to PAUSED mid-run → agents stop/re-check. Agent output tagged with project lifecycle state.
