# Codex Global Project Rules

> ⚠️ 这些规则适用于 Codex 创建**所有新项目**。不要跳过。

---

## 核心规则

### 1. 禁止从空白自由设计项目
Codex 默认**不要**从空白自由设计项目架构、技术栈、目录结构。
必须优先使用 `C:\Codex_App_Factory` 中已有的 starter、blueprint、skill。

### 2. 新项目必须先判断应用类型
每次接到新项目需求，第一步不是写代码，而是：
- 读取 `APP_TYPE_ROUTER.md` 判断项目类型
- 选最匹配的 starter
- 选最匹配的 blueprint
- 选相关的 skills

### 3. 新项目必须先输出设计稿，再写代码
不要跳过设计阶段直接写代码。设计稿必须包含：
- 应用类型判断
- 推荐技术栈
- 页面结构
- 数据结构（草图）
- API 结构（草图）
- 权限边界
- 第一版最小闭环
- 暂不实现的内容
- 风险点
- 需要用户确认的问题

### 4. 小项目不要微服务
开一个仓库、一个项目文件夹就行。不要拆成多个微服务仓库，除非用户明确要求"微服务架构"。

### 5. 第一版先做最小闭环
只做能让用户完成一个**真实目标**的最小功能集合：
- 1-3 个核心页面
- 1-2 个核心 API
- 最少必要数据表
不要追求大而全。

### 6. 默认不加以下内容
除非用户**明确书面要求**，第一版默认**不要**添加：
- 支付系统
- 分账系统
- 消息队列（Kafka、RabbitMQ 等）
- 多租户架构
- 复杂 AI 异常检测
- 复杂 AI 管道
- 分布式事务

### 7. 所有数据库项目必须有审计字段
每个数据表必须包含（至少）：
- `id`（主键）
- `created_at`（创建时间）
- `updated_at`（更新时间）
涉及删除的表加：
- `deleted_at`（软删除时间）

### 8. 所有登录/权限必须服务端校验
- 前端隐藏按钮 ≠ 权限控制
- 每个需要权限的 API 必须在服务端校验 token/role
- 不信任前端传来的任何权限声明

### 9. 所有移动端/小程序项目禁止把密钥放前端
- API 密钥、数据库密钥、第三方服务密钥只存服务端
- 小程序使用 OPENID 由服务端处理
- 前端只做展示和 API 调用

### 10. 所有 Web 项目必须考虑移动端基本适配
- 使用响应式布局
- 核心页面在 375px 宽度下可正常使用
- 按钮和链接至少有 44x44px 点击区域

### 11. 所有项目必须能用一条命令运行或预览
- 在 README 中写清楚：一条命令即可启动
- 如果有多步骤，提供脚本或简化说明

### 12. 完成后输出简短里程碑报告
不要长篇审计。报告只需包含：
- 创建/修改了哪些文件
- 如何运行
- 已实现的功能列表（3-5 条）
- 下一步建议（可选）

---

## 违规处理

如果 Codex 违反了以上规则，用户只需说：
- "先走 Codex_App_Factory"
- "先做设计稿"
- "不要过度设计"

Codex 应立即回到规则流程。
